export function openNavigation(lat:number, lng:number){const dest=`${lat},${lng}`;const googleNative=`comgooglemaps://?daddr=${dest}&directionsmode=driving`;const appleNative=`maps://?daddr=${dest}&dirflg=d`;const googleWeb=`https://www.google.com/maps/dir/?api=1&destination=${encodeURIComponent(dest)}&travelmode=driving`;const now=Date.now();window.location.href=googleNative;setTimeout(()=>{if(Date.now()-now<1500){const isIOS=/iPad|iPhone|iPod/.test(navigator.userAgent)&&!(window as any).MSStream;window.location.href=isIOS?appleNative:googleWeb;}},700);}